# Java Maven CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/openjdk:8-jdk
      
      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    # working_directory: /opt/apache-maven-3.5.0/conf/
    # steps:
    #    - run: cd /opt/opt/apache-maven-3.5.0/conf/
    #    - run: touch set.xml

    # environment:
    #   # Customize the JVM maximum heap limit
    #   JVM_OPTS: -Xmx3200m
    #   TERM: dumb
    
    steps:
       - checkout
       - run: mvn integration-test
      #  - run: mvn sonar:sonar
       - run: mvn validate
       - run: mvn clean package

       - store_artifacts:
          path: ~/sunil-circleci/spring-boot-students-api/

      #  - run: curl -uadmin:admin -T "~/sunil-circleci/vehiclemanagerms/.jar" "https://tcartifactory.jfrog.io/tcartifactory/libs-snapshot-local/com/tc/tsp/dcm-management/1.0.0-SNAPSHOT.jar"